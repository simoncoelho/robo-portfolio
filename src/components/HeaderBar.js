// HeaderBar.jsx
import React from "react";
import "./HeaderBar.css"; // the same CSS from above or inline styles

export default function HeaderBar() {
  return (
    <header className="header-bar">
      <div className="logo">simon coelho</div>
    </header>
  );
}